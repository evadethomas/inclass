#include <stdlib.h>

#include "main.h"

int main(int argc, char *argv[]) {
    int board_size = atoi(argv[1]);
    board_t board = malloc(sizeof(char) * board_size * board_size);

    board[r][c] = argv[i];
}
