#include <stdio.h>

int main(int argc, char* argv[]) {

    //1 + 2
    //TOKENS
    //OP_PLUS as root node 

    switch (argv[2][0]):
    	case '+': return eval(node->left) + node->right->value;
    	case '-': if (node->type is oper2) {
    				return atoi(argv[1])-atoi(argv[3);
    			} else {
    				return -(node->operand->value);
    			}
    	case '>>': return (atoi(argv[1]) >> atoi(argv[3))
    	case '~': return ~(node->operand->value)
}